{
  "private": true,
  "scripts": {
    "build": "webpack --mode=production",
    "lint": "prettier --check source/**/*.{js,css,html}",
    "lint-fix": "prettier --write source/**/*.{js,css,html}",
    "test": "npm run lint",
    "watch": "webpack --mode=development --watch"
  },
  "dependencies": {
    "webext-options-sync": "^2.0.1",
    "webextension-polyfill-ts": "^0.25.0"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^5.0.3",
    "prettier": "^2.2.1",
    "size-plugin": "^1.2.0",
    "terser-webpack-plugin": "^1.3.0",
    "webextension-polyfill-ts": "^0.25.0",
    "webpack": "^4.36.1",
    "webpack-cli": "^3.3.6"
  }
}
